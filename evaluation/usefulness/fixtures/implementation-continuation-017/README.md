# Default configuration continuation fixture

The repository test defines the next behavior. Identify the smallest change to
complete the default configuration helper and the focused verification. This
fixture is read-only.
